<div class='flex items-center max-w-sm w-full mb-12'>
  <%= avatar(@user) %>

  <h3 class='text-gray-900 text-xl leading-none'><%= @user.username %></h3>
  <%= follow_button @user %>
  <%= link_to 'Followers', user_followers_path(@user) %>
</div>

<%= render @posts %>
