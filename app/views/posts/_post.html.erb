<div class='w-full block lg:max-w-full mt-12 mb-6lg:flex'>
  <%= avatar post.user %>
  <%= link_to post.username, post.user %>
  <%= render post.content %>
  <%= link_to post do %>
    <%= time_ago_in_words(post.created_at) %> ago
  <% end %>

  <%= like_button(post) %>
</div>